@using Newtonsoft.Json
@using Newtonsoft.Json.Serialization
@model Angelo.Connect.Video.Models.VideoWidgetViewModel

@{ 
    var modelText = ViewData["modelText"] as string;
}

<pre>@modelText</pre>

