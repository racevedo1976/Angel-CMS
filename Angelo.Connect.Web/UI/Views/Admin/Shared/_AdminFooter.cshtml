@using Microsoft.AspNetCore.Builder
@using Microsoft.AspNetCore.Http.Features
@using Microsoft.AspNetCore.Localization
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Options

@inject IConfigurationRoot config
@inject IOptions<RequestLocalizationOptions> LocalizationOptions

@{
//var requestCulture = Context.Features.Get<IRequestCultureFeature>().RequestCulture;
//var supportedCultures = LocalizationOptions.Value.SupportedCultures
//    .Select(c => new SelectListItem { Value = c.Name, Text = c.DisplayName })
//    .ToList();
}

<br />
<br />
<div id="footer" class="footer-content navbar navbar-fixed-bottom clearfix">
    @*<p class="pull-left">
        @Html.Localize("Language:") 
        <a bs-toggle-modal="cultureOptions">
            @requestCulture.Culture.DisplayName
        </a>
    </p>
    <p class="pull-right">
        <i class="glyphicon glyphicon-flash"></i> &nbsp;
        @Html.Localize("Powered By") @config["Application:Name"] @config["Application:Version"]
    </p>*@
</div>

@*<modal id="cultureOptions" title="@Html.Localize("Select Preferred Language")">
    <modal-body>
        <form class="form" id="cultureOptionForm" asp-controller="User" asp-action="Culture">
            <div class="form-group">
                <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                <select class="form-control" name="culture" asp-for="@requestCulture.UICulture.Name" asp-items="supportedCultures" onchange="cultureOptionForm.submit()"></select>
            </div>
        </form>
    </modal-body>
</modal>*@